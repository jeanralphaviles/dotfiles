- defaults:
    link:
      create: true
      force: true
      relink: true

- clean: ['~']

- link:
    ~/.i3:
        path: i3/
    ~/.i3status.conf: i3status.conf
    ~/.tmux.conf: tmux.conf
    ~/.vim/after: vim/after
    ~/.vimrc: vimrc
    ~/.zshrc: zshrc
